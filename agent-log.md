# Agent Decision Log

Append-only. One entry per consequential decision, newest first. Format:
**Situation** (what was true) · **Decision** (what we chose) · **Risk** (what could bite) · **Change** (what moved in the repo).

---

## 2026-06-15 — Camp → two SKUs: Single morning $50 / Full week $150 (payments / IPAV / gauntlet #2)
- **Situation:** Hours after shipping the camp as a flat $50/week (#196), Sam corrected: $50 is **per morning**, with a $150 full-week cap. Second `/gauntlet` (report `~/.claude/usage-data/gauntlet-nga-camp-per-day-2026-06-15.md`) flagged the day-picker model he first picked as misleading (3 days = 4 days = $150) + scope-creep; 4/5 critics recommended **two flat SKUs**. Sam chose two SKUs (Single morning $50 / Full week $150) with a which-morning selector on the single-day SKU.
- **Decision:** `CAMP_OPTIONS` = `day`($50, `STRIPE_CAMP_DAY_PRICE_ID`) + `week`($150, `STRIPE_CAMP_WEEK_PRICE_ID`); `CampOptionKey="day"|"week"`. New `campDays(camp)` helper (UTC-safe ISO arithmetic, single source for form + validator). `validateCampForm` requires `selectedDay ∈ campDays(camp)` for the day SKU (rejects empty/cross-week/garbage → no $0-free-camp or wrong-bundle). Each SKU = one Stripe line item qty 1 (no cart math, no 3=4 trap). checkout-camp bakes the chosen day into `option_label` ("Single morning — Thu, Jul 2") so the webhook/email/CRM/success read it verbatim — **zero webhook edits** to the slop-free file. Stripe: reused $50 price `price_1TidG4BpXOfTC961p6oENUML` as DAY (renamed), created $150 WEEK `price_1TigtMBpXOfTC961xnZ1c27R`; both env vars set in Vercel before merge.
- **Risk:** Slop-free checkout-camp edited (metadata-only: `option_label`+`selected_day`; price resolution unchanged). Display-vs-charge guarded by a live dual probe ($50 day / $150 week) before crediting. Newsletter cron `Math.min(CAMP_OPTIONS)`=50 → "from $50/day". Copy framed as a price **cap** ("4th morning on us"), not "save $50" (brand transparency).
- **Change:** `camps.ts`, `validate-camp.ts`, `CampRegisterForm.tsx` (day `<select>`), `checkout-camp/route.ts`, `camp/page.tsx`, `camp/[slug]/page.tsx`, `weekly-newsletter.ts`, `.env.example` + rewritten `validate-camp.spec.ts` (two-SKU set, campDays, selectedDay edge cases) + `weekly-newsletter.spec.ts`. Fail-before/pass-after; mutation-verified (week→140 + weakened day-guard each fail a spec). Full suite 555 pass / 8 skip + lint + funnel + build green.

---

## 2026-06-15 — Camp → morning-half-day-only @ $50/week (payments / IPAV / gauntlet)
- **Situation:** NGA summer camp (Wk1 Jun 29, Wk2 Jul 20) had zero registrants and rented morning courts. Sam decided to collapse the product to a single **morning half-day** option at a standing **$50/week** and market it. Plan run through `/gauntlet` (GO-WITH-CHANGES): report `~/.claude/usage-data/gauntlet-nga-camp-50-am-only-2026-06-15.md`, Plan v2 `~/.claude/plans/nga-camp-50-am-only-v2.md`.
- **Decision:** `CAMP_OPTIONS` → single `am` entry at `priceUsd:50`; `CampOptionKey` narrowed to `"am"` (dead full/pm refs fail typecheck, not silent 503). Form default `optionKey:"am"`. Parent-email lunch line (am campers leave 12:30) → "A morning snack". Metadata + newsletter copy → morning-half-day. Charge change is the Stripe price object, NOT `priceUsd` — new $50 price `price_1TidG4BpXOfTC961p6oENUML` created on `prod_UdZrVIQyygPk87`; `STRIPE_CAMP_AM_PRICE_ID` repoint + archive-old deferred to ship time (repoint-before-deploy would mismatch the live build).
- **Risk:** Slop-free payments files touched — webhook (lunch line only; #194 idempotency ledger confirmed intact on origin/main) + checkout-camp path (validation-before-env ordering preserved → removed keys 400, not 503). Gauntlet criticals addressed by sequencing (charge-live-before-display, before Thu Jun 18 newsletter cron) + a live $50 checkout probe gate before any marketing. Marketing gated separately: ELIGIBLE+subscribers list only (DD guardrail), "Gaithersburg, MD" only (child-safety), /brand-review-nga.
- **Change:** `camps.ts`, `CampRegisterForm.tsx`, `validate-camp.ts`, `webhook/route.ts`, `camp/page.tsx`, `camp/[slug]/page.tsx`, `weekly-newsletter.ts` + load-bearing tests (`validate-camp.spec.ts`: am-only set / full+pm undefined / $50; `weekly-newsletter.spec.ts`: `Math.min(CAMP_OPTIONS)===50`, replacing the tautological input assertion). Tests fail-before/pass-after; mutation-verified (price→170 fails 2 specs). Full suite 551 pass / 8 skip + lint + funnel + `next build` green.

---

## 2026-06-14 — Webhook camp/league idempotency via shared processed-events ledger (deferred §8a, Surface 2)

- **Situation:** `handleCampCheckout` / `handleLeagueCheckout` (`src/app/api/stripe/webhook/route.ts`, Slop-Free payments) had no dedupe store — unlike drop-in (`findDropInByCheckoutId`) and cluster (`findClusterRegByCheckoutId`), both keyed on the checkout-session id. Camp/league have no roster row of their own, so a Stripe redelivery of a 200'd `checkout.session.completed` re-fired the parent + admin confirmation emails. Second deferred surface in `docs/source-inventory.md §8a` (payments-class, its own IPAV go).
- **Decision (Sam: chose shared-dedupe-DB via AskUserQuestion, then "go for surface 2"):** A single shared processed-events ledger instead of two full roster DBs (lighter, reusable, smallest payments blast radius; the roster DBs belong with the community-os growth app). New `src/lib/notion-processed-events.ts` (`findProcessedEvent` + `recordProcessedEvent`) mirrors `notion-clusters.ts` exactly, returning `CreateDropInResult`. Surgical guard at the TOP of both handlers: find → idempotent no-op; record on the sync path before `after()`; **transient** record-fail → 500 (Stripe redelivers, find-guard makes retry safe); **permanent**/env-unset → proceed with comms (a paid family's only confirmation must never be stranded by a broken ledger). Fail-soft to `"ok"`/`false` when `NOTION_PROCESSED_EVENTS_DB_ID` is unset → camp/league behave exactly as today until Sam creates the DB (PR is inert on merge).
- **Risk:** Touches the Slop-Free webhook payments file — but only adds a top-of-handler guard; the existing `after()` comms block, return shapes (`{received:true, camp|league:true}`), and child-PII flow are untouched. Two extra Notion calls on the sync path before ack (same cost drop-in/cluster already pay; well under Stripe's timeout given the fast 200). Permanent-failure-proceeds means a genuinely broken ledger silently disables dedupe (logged loudly) rather than dropping confirmations — deliberate trade (a missed dedupe resends one email; a dropped email strands a paid family). `new Date().toISOString()` (timestamp, not date-only) passed in from the route, so no UTC date-only hazard.
- **Change:** new `src/lib/notion-processed-events.ts`, guard added to `handleCampCheckout` + `handleLeagueCheckout` in `src/app/api/stripe/webhook/route.ts` (+ import), `.env.example` +`NOTION_PROCESSED_EVENTS_DB_ID`, new `e2e/invariant-webhook-camp-league-idempotency.spec.ts` (redelivered no-op / first-delivery records once / transient→500, ×camp ×league). Mutation-checked both guards (find + transient-500) load-bearing. Full `test:pure` (529) + lint + build green. Before live: Sam creates the Notion DB + sets the env var; merge is a no-op until then.

## 2026-06-14 — Agent trigger parity for crew confirmation (deferred §8a, Surface 1)

- **Situation:** `confirmCrewAction` (`src/app/coach/(authed)/polls/[slug]/actions.ts`) inlined its whole fan-out — coach auth, per-parent Resend crew-confirmed email, poll `Status → Confirmed` write, `revalidatePath` — inside a `"use server"` action. Like attendance pre-#189/#191, there was no agent path: an out-of-band confirm would have to PATCH the Notion Status directly and silently drop the parent emails. Logged as the first deferred surface in `docs/source-inventory.md §8a` (minor-PII, Slop-Free).
- **Decision (Sam: plan approved; Surface 1 only, Surface 2 a separate payments-class go):** Mirror the shipped attendance shape exactly. Extract a plain-lib core `confirmCrew()` (`src/lib/crew-confirm.ts`, NOT `"use server"`) holding the email loop + Status flip + the already-Confirmed/Cancelled early-return (now an explicit idempotency guard returning `{idempotent:true}`). `confirmCrewAction` becomes a thin `requireCoach() → confirmCrew() → revalidatePath` wrapper (click path byte-for-byte unchanged). New Bearer-gated `POST /api/coach/crew-confirm` uses `secretEquals` + a **dedicated** `CREW_CONFIRM_SECRET` (not `NGA_ADMIN_SECRET`), fails closed when unset, returns a PII-free ack (counts only). Deliberately did NOT add OB ingest — the UI path doesn't ingest, so parity means the agent doesn't either (possible follow-up).
- **Risk:** The route grants crew-confirm (which emails parents minor-PII) to any `CREW_CONFIRM_SECRET` holder, bypassing `COACH_ALLOWED_EMAILS` — same trust shape as the attendance route, with the same hardened header transport + dedicated-secret isolation. Secret must be set in Vercel before the route is live (unset = 401, so it fails safe). Unlike the action, the route does not `revalidatePath` (no coach session to refresh; coach view picks up the flip on next ISR) — an agent confirm won't instantly refresh an open coach tab.
- **Change:** new `src/lib/crew-confirm.ts` (core + idempotency guard), `src/app/coach/(authed)/polls/[slug]/actions.ts` (thinned to auth+revalidate wrapper), new `src/app/api/coach/crew-confirm/route.ts`, `.env.example` +`CREW_CONFIRM_SECRET`, new invariant specs `e2e/invariant-crew-confirm-trigger-parity.spec.ts` (Bearer gate fail-closed + email/Status parity + idempotent no-op) and `e2e/invariant-crew-confirm-pii-egress.spec.ts` (egress hosts + PII-free ack). Mutation-checked all three guards (gate, idempotency, email fan-out) load-bearing; full `test:pure` (523) + lint + build green. Draft PR per surface; Surface 2 (webhook camp/league idempotency) paused for its own go.

## 2026-06-14 — Harden the agent attendance route (gauntlet fixes onto #189)

- **Situation:** Two parallel sessions independently built attendance trigger-parity. #189 (shipped to main: `applyAttendance` + `/api/coach/attendance`) and #190 (gauntlet-reviewed: `markAttendanceCore` + `/api/attendance`, closed as the duplicate). #189 shipped with the weaker posture the gauntlet had flagged on #190: route gated by `NGA_ADMIN_SECRET` via `?secret=` query param (mega-secret blast radius + secret-in-access-logs, hostile-reviewer #9/#10), OB ingest left `void` fire-and-forget even on the route (floating promise can die on a Vercel freeze before it flushes), and no idempotency guard (agent retries append duplicate OB check-in activities). #189 also skipped governance docs for its new lib/route/secret.
- **Decision (Sam: "Harden #189"):** Keep #189's structure (`applyAttendance`, `/api/coach/attendance`) and port the three gauntlet fixes onto it rather than re-landing #190. (1) Route auth → `Authorization: Bearer ATTENDANCE_SECRET` — dedicated secret (already set in Vercel prod+preview this session), header transport, fail-closed when unset. (2) `applyAttendance` returns the (never-throwing) OB ingest promise; the route `await`s it for durability, the coach action still ignores it (fire-and-forget UX unchanged). (3) Idempotency guard: if the row already holds the requested value, no-op (no write, no OB, no recompute) → `{idempotent:true}`. Plus PII-free ack (status only) + a child-PII egress invariant.
- **Risk:** Switching the route secret from NGA_ADMIN_SECRET to ATTENDANCE_SECRET would break any existing caller — but the route shipped <1d ago with no known caller, and ATTENDANCE_SECRET is live in Vercel. Route still grants attendance-write to any ATTENDANCE_SECRET holder, bypassing `COACH_ALLOWED_EMAILS` (logged source-inventory §8 #9) — same trust shape as the cancel-registration admin curl, now with the hardened transport. The idempotency guard required fixing #189's existing parity-test fixture (it started `Attendance: Present` and requested Present → would now no-op); changed the fixture to start unrecorded.
- **Change:** `src/lib/attendance.ts` (idempotency guard + return obIngest), `src/app/api/coach/attendance/route.ts` (Bearer auth + await OB + PII-free ack + 404 on not-found), `e2e/invariant-attendance-trigger-parity.spec.ts` (Bearer gate + fixed fixture + idempotency + clear cases), new `e2e/invariant-attendance-pii-egress.spec.ts`, plus the governance docs #189 omitted (`.env.example` +ATTENDANCE_SECRET, CLAUDE.md env + Slop-Free minor-PII list + coach-dashboard note, source-inventory §2/§3/§7/§8). #190 closed as superseded. Mutation-checked the 3 triggers; full `test:pure` + lint + build green. Follow-up PR (the feature's first PR was #189) → auto-merge per defaults.

## 2026-06-13 — Backfill script reaches webhook parity (session count + Player CRM)

- **Situation:** `scripts/backfill-dropin.mjs` only created the roster row — it skipped the webhook `after()` block's `incrementSessionRegistered` and `syncPlayerFromDropIn`. So every hand-backfill silently under-counted the session (wrong fill meter / a Full session reads Open) and left the registrant out of the Player CRM. Surfaced when the Landon backfill (same day) drifted WJHS 6/14 to 5-vs-6.
- **Decision:** Port both side-effects into the script as best-effort, non-throwing steps that run only after a *new* roster row is created (the existing idempotency guard exits before them on re-run, so no double-count). Kept the repo's standalone-`.mjs` convention (no tsx; the libs import via `@/` alias) — raw-`fetch` ports faithful to the source functions, each commented "KEEP IN SYNC with src/lib/…". Player-CRM write is the SAME egress + SAME child fields (first name + birth-year→age) the webhook already uses — not a new PII destination.
- **Risk:** Duplicated logic can drift from `notion-sessions.ts` / `notion-player-sync.ts` (mitigated by sync-comments + the fact the pure helpers are unit-tested in the lib). Faithful-port caveat: like the live webhook, `findPlayerRow` matches on Parent Email/Phone, so an eval-created CRM row lacking contact info won't be found and a new player row is created — pre-existing lib behavior, deliberately not "fixed" here to avoid divergence.
- **Change:** `scripts/backfill-dropin.mjs` only (no app code, not a slop-free-zone TS file). `node --check` + eslint clean; both ported flows validated READ-ONLY against live Notion (increment math on the real session row; `findPlayerRow` query shape). No script-level test harness exists in the repo (convention); logic mirrors lib functions already covered by `e2e/notion-{sessions,player-sync}.spec.ts`.

## 2026-06-13 — Drop-in roster write is fail-soft on the optional `Source` column

- **Situation:** #174 added a `Source` select write to the drop-in roster create, but the `Source` property was never created on the NGA Drop-in Registrations Notion DB. From the 6/12 deploy on, every paid drop-in `checkout.session.completed` got a deterministic Notion 400 → `"permanent"` → paid-but-unregistered + alert email. First (and only) victim: Landon Lee, 6/13, for the 6/14 WJHS session. Root cause closed separately by adding the `Source` column to the DB; this hardens the code so a missing/renamed optional column can never again strand a registration.
- **Decision:** In `createDropInRegistrationResult`, if the create fails with a *permanent* status AND `Source` was in the payload AND the error body names `Source`, drop `Source` and retry the create once. The core roster row (source of truth for reminders/check-in/cancel refunds) lands; attribution degrades to blank. Happy path is unchanged (single call, Source included). Scoped tightly to Source-named errors so a real permanent failure (bad Status, etc.) is NOT masked.
- **Risk:** Touches `notion-dropins.ts` (Minor-PII slop-free zone) — but only the create's failure path; no change to the property set on success, idempotency, child-PII flow, or refund logic. The substring match (`bodyText.includes("Source")`) could in theory match a Source-bearing parent name in an unrelated 400 → at worst one harmless retry-without-Source; it never suppresses a non-Source failure (pinned by test).
- **Change:** `src/lib/notion-dropins.ts` (extracted `postPage`, added the one-shot Source-stripping retry). New pins in `e2e/notion-dropins.spec.ts` (4 specs: retries-without-Source-and-lands, happy-path-single-write, non-Source-permanent-not-masked, transient-not-retried). Mutation-checked both the fail-soft behavior (red before fix) and the no-mask guard (forced retry-on-any-permanent → guard fails). Full `npm run test:pure` (508) + lint + build green.

## 2026-06-12 — Secret split completed for referral + newsletter-unsub; all 5 token secrets live in Vercel (#181)

- **Situation:** After #180, referral and newsletter-unsub tokens still verified single-secret — setting their (already-documented) env vars would have broken every unsubscribe link in past issues and every referral link in old welcome emails. Both vars were unset in Vercel.
- **Decision:** Same `signingSecrets` dual-verify as the other three families; then ALL FIVE dedicated secrets set in Vercel (Sensitive, Production-only, mirroring NGA_ADMIN_SECRET scope) and prod redeployed. Sequencing mattered: the Vercel "Redeploy" toast targeted the pre-#181 deployment — redeploying THAT with the new vars would have run single-verify code with a dedicated secret set (the exact brick this PR prevents). Redeployed the #181 build explicitly instead.
- **Risk:** Legacy-era links now ride the fallback path until NGA_ADMIN_SECRET rotates (intended). Pre-existing CI flake fixed in passing: coach-auth tamper tests replaced the last char with a FIXED char, silently no-op'ing ~1/64 runs (token embeds Date.now()).
- **Change:** `src/lib/{referral-token,newsletter-token}.ts`, 5 new invariant tests, de-flaked coach-auth.spec.ts, `.env.example` comments. Secrets recorded in the operator env registry.

## 2026-06-12 — Security hardening: timing-safe gates + NGA_ADMIN_SECRET split (Sam-approved slop-free edits)

- **Situation:** Risk log #1–3: all 14 route-gate secret compares were plain `!==` (timing side-channel), and NGA_ADMIN_SECRET both gated 5 admin routes (2 refund-capable) and signed all 5 HMAC token families — one leak forged everything. Sam approved both fixes ("yes do both").
- **Decision:** New `src/lib/secret-compare.ts` (`secretEquals` constant-time + fail-closed; `signingSecrets` dedicated-first key resolution). All 14 gates swapped. cancel/session-cancel/commit tokens sign with dedicated secrets when set, **verify against dedicated AND legacy** — outstanding non-expiring links in parent inboxes survive the env-var flip. Tests written first (3 failed against old behavior), source-level mutation check confirmed they're load-bearing.
- **Risk:** Until the new env vars are set in Vercel, signing still uses the legacy key (no behavior change at deploy — by design). The legacy-era same-key edge (a session-cancel token decodes as a `session:`-prefixed garbage cancel id, bounded to a no-op lookup) is pinned by test and disappears once distinct secrets are live. The `?secret=` query-param fallback on notion-session-webhook is retained deliberately (Notion header limitation).
- **Change:** `src/lib/{secret-compare,cancel-token,session-cancel-token,commit-token}.ts`, 14 route files (compare swap + import only), `.env.example` (+3 vars), `e2e/invariant-secret-separation.spec.ts` (10 tests). Operator follow-up: set `CANCEL_TOKEN_SECRET` / `SESSION_CANCEL_TOKEN_SECRET` / `COMMIT_TOKEN_SECRET` in Vercel + both env registries when ready.

## 2026-06-12 — Verification harness pins youth-data invariants as pure specs

- **Situation:** 41 page routes, 40 API routes, all child data flowing Stripe→Notion with HMAC-token/cookie gates; 41 existing pure specs covered templates and helpers but nothing pinned the auth gates, webhook idempotency, consent mapping, or child-PII egress. CI runs `test:pure` only.
- **Decision:** Extend the playwright.pure pattern with 10 invariant specs + a throw-on-unstubbed global-fetch stub and offline Stripe signature generation, so the highest-stakes behavior runs deterministically in CI on every PR. Tests observe slop-free files; zero production edits.
- **Risk:** Route handlers register `after()` comms that may throw outside a Next request scope — specs settle either way and assert on captured calls, so a future Next behavior change could silently skip response-status assertions on the create path. Twilio rides its own SDK transport, so the egress spec's host allowlist covers fetch-based egress only (SMS is separately consent-hard-gated).
- **Change:** `e2e/fixtures/{fetch-stub,stripe-sessions}.ts`, 10 `e2e/invariant-*` / `webhook-*` specs (44 tests), all passing with the existing 430.

## 2026-06-12 — Mutation check proves the new specs are load-bearing

- **Situation:** A test that cannot fail proves nothing; 4 prior sessions established the pattern of verifying failure states explicitly.
- **Decision:** Perturb three fixtures (properly-signed rebind token expected null; valid webhook signature expected 400; exact-string consent expected false) and require the suite to FAIL, then restore green.
- **Risk:** Fixture-side mutation proves assertions are live but not that every source regression is caught — source-side mutation was skipped because all guarded files are slop-free.
- **Change:** No repo change; mutation run failed 3/3 as required, restore run 19/19 green.

## 2026-06-12 — Governance docs anchored to the audited tree, counts derived at runtime

- **Situation:** A prior community-os audit (same day) showed hardcoded verification counts were wrong at planning time (crons 27≠26, p3 routes ≠35) and that `src/app` vs `app` layout splits silently zero out naive globs.
- **Decision:** `docs/source-inventory.md` states counts as "@ a591bbd, re-derive via git ls-tree"; classification carries evidence per verdict; risk log documents rather than fixes (fixes are separate approvals).
- **Risk:** The inventory goes stale as main moves; staleness column dates are squash-PR dates, not semantic change dates.
- **Change:** `docs/source-inventory.md`, `docs/hostile-reviewer.md`, CLAUDE.md governance sections, `skills/` (3 procedures).

## 2026-05-30 (backfilled from code) — Cancel tokens are non-expiring and keyed on NGA_ADMIN_SECRET

- **Situation:** Parents cancel anywhere from a week out to morning-of; an expiry window adds friction without real protection.
- **Decision:** Non-expiring HMAC token binding exactly one checkout-session id; signing key reuses `NGA_ADMIN_SECRET` to avoid a new env var (rationale inline at `src/lib/cancel-token.ts:14-21`).
- **Risk:** `NGA_ADMIN_SECRET` is a mega-secret — bearer gate for 5 admin routes (2 refund-capable) AND the HMAC key for all 5 token libs. One leak = forge any token + issue refunds. Rotation invalidates every outstanding link (pinned by test).
- **Change:** Documented in `docs/source-inventory.md` risk log #1; revisit key separation if blast radius grows.

## 2026-06-03 (backfilled from code) — Webhook acks fast; comms ride after()

- **Situation:** Awaiting every email/SMS/coupon before 200'ing blew Stripe's webhook timeout on cold starts → retry storm, ~50% error rate.
- **Decision:** Create the Notion roster row (the only critical write) on the response path; everything else runs via `after()` post-response with `maxDuration: 60`. Transient Notion failures 500 (Stripe retries); permanent ones alert + ack (operator backfills via `scripts/backfill-dropin.mjs`).
- **Risk:** `after()` work that throws is lost silently — a failed parent confirmation email leaves a paid-but-unnotified registration (admin email is the catch).
- **Change:** Documented at `src/app/api/stripe/webhook/route.ts:33-39, 394-418`; idempotency + transient/permanent split pinned by `e2e/webhook-idempotency.spec.ts`.

## 2026-06-13 — WhatsApp parent-group invite on every registrant email

- **Situation:** The Next Gen parent WhatsApp invite only rode along on a parent's *first* touch (booking confirmation gated by `isFirstTimer`, lead/Yellow Ball confirmations, newsletter welcome). Sam wants every *registered* family to have a standing way to reach Coach Sam and each other.
- **Decision:** Render the existing (brand-approved) `whatsappInviteHtml()`/`whatsappInviteText()` block unconditionally on all session-related registrant emails: booking confirmation, day-before reminder, post-session recap, and no-show rebook. Excluded cancellation/refund comms (off-tone) per Sam. Lead/Yellow Ball confirmations left first-timer-gated (leads aren't registrants). Removed the now-dead `isFirstTimeParent()` lookup + `parentEmailForLookup` from the Stripe webhook (helper still used by yellowball-lead).
- **Risk:** Edits touch the payments webhook (slop-free zone), but only drop a Notion read + flip a copy flag — no change to roster write, idempotency, refunds, or PII flow. No new child-PII egress. Copy itself unchanged (placement only), so no new brand-review surface.
- **Change:** `src/app/api/stripe/webhook/route.ts`, `src/lib/email/{booking-confirmation,booking-reminder,post-session,post-session-rebook,whatsapp-invite}.ts`; new pin `e2e/whatsapp-invite.spec.ts` (4 specs). Full `npm run test:pure` (493) + lint + tsc green; webhook build blocked only by sandboxed Google Fonts fetch.

## 2026-06-15 — Attendance trigger parity: shared core + agent-callable route

- **Situation:** An agent marked day-of attendance by writing the Notion `Attendance` select directly (via MCP), which skipped the rest of `markAttendanceAction`'s fan-out — the `ingestToOpenBrain({ source: "nga_attendance" })` check-in activity AND the `recomputePlayerAttendance` player-profile stat refresh. The triggers lived only inside the coach-cookie server action, unreachable except by clicking the button.
- **Decision:** Extracted the full fan-out into one shared `applyAttendance()` core (`src/lib/attendance.ts`): find row → `setDropInAttendance` → OB ingest → player recompute. `markAttendanceAction` now delegates to it (keeps coach-cookie auth + request-scoped revalidate). Added `POST /api/coach/attendance`, the agent-callable twin, gated by `NGA_ADMIN_SECRET` (constant-time `secretEquals`, fails closed) — so a back-office agent fires the identical triggers instead of touching the raw store. Mirrors the existing `cancelDropIn` single-entry-point shape.
- **Risk:** Minor-PII/coach surface (IPAV). Did NOT edit the observe-only libs (`notion-dropins`, `notion-player-sync`, `open-brain-ingest`) — only imported. `markAttendanceAction`'s external behavior is byte-preserved (same messages, same validation order, same auth). New secret-gated route is a new egress path for the same child fields already flowing through the action — no NEW destination (OB + Notion were already the targets).
- **Change:** New `src/lib/attendance.ts`, `src/app/api/coach/attendance/route.ts`; refactored `src/app/coach/(authed)/[slug]/actions.ts`; new pin `e2e/invariant-attendance-trigger-parity.spec.ts` (4 specs: 3 fail-closed auth + 1 trigger-parity, mutation-verified — parity spec fails when OB ingest is removed). Full `npm run test:pure` (512) + lint + tsc green; `next build` blocked only by the sandboxed Google Fonts fetch.

## 2026-06-15 — crew-autoreserve Stripe idempotency key (payments / IPAV)
- **Situation:** crew-autoreserve charges a saved card off-session ($20) once per (commit, session). Its only duplicate guard was an eventually-consistent Notion roster lookup (`processOne` `already` check) — a same-day cron re-run (Vercel retry / overlapping invocation) can pass it before the first run's roster row is visible and double-charge. Surfaced by the gauntlet payments critic ahead of arming the cron.
- **Decision:** Added a deterministic Stripe idempotency key per (commit, session). New pure lib `src/lib/crew-charge.ts` → `crewChargeIdempotencyKey(commitId, sessionId)` = `crew-autoreserve-<commit>-<session>`; passed as the 2nd arg (`RequestOptions`) to `stripe.paymentIntents.create(...)`. Stripe dedupes identical keys for 24h → a duplicate create() no-ops at the payment layer regardless of Notion read lag.
- **Risk:** Slop-free payments file (`api/cron/crew-autoreserve`). Charge **params are byte-unchanged** — only the 2nd options arg was added, so amount/customer/PM/off_session/confirm/metadata are identical. Inert today (0 Active commits). No behavior change beyond dedup.
- **Change:** New `src/lib/crew-charge.ts` + `e2e/crew-charge-idempotency.spec.ts` (4 specs: determinism + differs-by-session + differs-by-commit + embeds-both-ids; mutation-verified — dropping sessionId fails 2 specs). One-line wiring in `route.ts`. Full `npm run test:pure` (533) + lint + `next build` green.

## 2026-06-16 — Admin session Cancel & Reschedule (gauntlet)
- **Situation:** /admin/sessions could only edit Notion fields; setting Status=Cancelled or changing the date silently skipped refunds + parent comms. Real cancel (refund+notify) lived only on /coach/[slug].
- **Decision:** Add admin Cancel (reuse executeSessionCancel) + Reschedule (carry-over, email-only). Reschedule degrades by roster size; >0 roster migrates each drop-in row Session Date/Start Time + resets Reminder/Post-Session/Reschedule-Notified flags; two-phase abort-before-email; seeded Tuesdays blocked; strictly-future guard. New Reschedule Notified Notion column (idempotency).
- **Risk:** payments-adjacent comms + minor-PII roster mutation (Slop-Free notion-dropins.ts). Mitigations: no Stripe import in reschedule (test-pinned), parent-only egress, two new admin routes fail-closed (invariant spec), mutation-checked.
- **Change:** new session-reschedule.ts + email/session-rescheduled.ts + 2 admin routes; notion-dropins.ts updateDropInSchedule + Reschedule Notified flag; SessionsEditor Cancel/Reschedule panels + Status->Cancelled footgun guard; e2e/session-reschedule.spec.ts + invariant-admin-session-routes-auth.spec.ts.

## 2026-06-17 — One-call cancel for multi-row session days (all-levels Tuesday / clusters)
- **Situation:** The all-levels Tuesday is four independent Sessions-DB rows (one court per level), not one event. On 2026-06-16 only the Orange row was flipped to Cancelled; Red/Green/Yellow stayed Open, and a family booked the still-Open Red court. `executeSessionCancel` cancels exactly one row, so "cancel the Tuesday" by hand is easy to half-do. (Checkout's live Status guard worked correctly — the Red row genuinely was Open.)
- **Decision:** Add a thin orchestration layer over the existing engine — no change to any money-moving code. `cancelAllLevelsForDate()` enumerates the date's level rows by title-prefix and fires `executeSessionCancel` once per non-terminal row (fail-soft per level). Surfaced three ways: agent/admin `POST /api/admin/sessions/cancel-all-levels` (same `authorizeSessionOps` gate as single cancel), and a coach `Cancel ALL levels this day` button on `/coach/[slug]` (shown only for `— <Level>`-suffixed multi-row days; passes the exact base title so clusters work too).
- **Risk:** Payments-adjacent (IPAV). Did NOT touch `session-cancel.ts`/Stripe/`notion-dropins` — only imported `executeSessionCancel`. Blast-radius guard = title-prefix match, so an unrelated same-date session can never be swept in; terminal rows (Cancelled/Completed/Passed) are skipped (no re-refund/duplicate email). New route fails closed. No new child-PII egress (same engine, same parent-only comms). Group cancel is re-runnable (per-row idempotent via existing flags + Stripe).
- **Change:** new `src/lib/session-cancel-group.ts`, `src/app/api/admin/sessions/cancel-all-levels/route.ts`, `src/app/coach/(authed)/[slug]/CancelAllLevelsButton.tsx`; wired `cancelAllLevelsAction` into `[slug]/actions.ts` + the level-group detection into `[slug]/page.tsx`; new pin `e2e/session-cancel-group.spec.ts` (6 specs: 3 fail-closed auth + per-level fan-out + terminal-skip + blast-radius + cookie parity; mutation-verified — defeating the title-prefix guard fails 3). Full `npm run test:pure` (587) + lint + tsc green; `next build` blocked only by the sandboxed Google Fonts fetch.

## 2026-06-17 — Crew Interest matching + follow-up automation
- **Situation:** Crew Interest submissions landed in Notion and got a one-shot welcome, then nothing — no matching, no nudge to Sam, no re-engagement. Sam wanted (1) finer skill granularity, (2) auto-matching, (3) timed follow-ups, (4) matching open sessions surfaced to parents.
- **Decision:** Added an optional low/mid/high **skill sub-level** child field (Sam-approved). New pure matching engine (`crew-matching.ts`): crew↔crew gate = same color + age ±3 + ≥1 shared day + area overlap (empty area = wildcard), sub-level only ranks; crew↔session = same level + preferred weekday + area overlap, open with a seat. Welcome email + new 7-day parent re-engagement email surface matching open sessions. New daily cron `/api/cron/crew-followup` (Bearer CRON_SECRET, `0 15 * * *`): day-3 → single internal digest to Sam (candidate matches + session-fit count, flips `Nudge Sent`); day-7 → parent re-engagement email (flips `Reengagement Sent` + `Nudge Sent`). Reengage wins when both due; reads only Status New/Reviewed so routed families are never re-touched.
- **Risk:** New child field = minor-PII governance trigger. Sub-level flows only on the existing crew-interest path (Notion + Resend + OB/cohort-pool, all already destinations) — NOT the registration/webhook path. New cron is a new surface reading child PII but egresses only to Notion + Resend with parent/admin-only recipients. Crew machinery is prototype-tier; changes are additive (3 nullable Notion columns, new files). Rollback = revert route diff (welcome → generic CTA), drop the cron from vercel.json, drop the columns.
- **Change:** New `crew-matching.ts`, `crew-followup.ts`, `email/{crew-followup-parent,crew-session-lines}.ts`, `api/cron/crew-followup/route.ts`; extended `validate-crew-interest.ts` (sub-level), `notion-crew-interest.ts` (sub-level write + `fetchActionableCrewInterest`/`markCrewInterestFlag`), `crew-interest/route.ts` (matched sessions in welcome), `email/crew-interest-welcome.ts`, `CrewInterestForm.tsx`, `vercel.json`. New pins: `crew-matching.spec.ts`, `crew-followup-logic.spec.ts`, `invariant-crew-interest-pii-egress.spec.ts`, `invariant-crew-followup-egress.spec.ts` + sub-level cases in `validate-crew-interest.spec.ts`. Added `Skill Sub-Level`/`Nudge Sent`/`Reengagement Sent` to the NGA Crew Interest Notion DB via MCP. Full `npm run test:pure` (608) + lint + tsc green; `next build` blocked only by the sandboxed Google Fonts fetch. **Prod prereq:** set `NOTION_CREW_INTEREST_DB_ID=b5b486c164254f03860a7693deaf2be3` (currently unset → Notion writes skip).

## 2026-06-17 — Refund→row-flip resilience (charge.refunded webhook + coach already-refunded detection)
- **Situation:** A live out-of-band refund (Stripe Dashboard / MCP / admin API) for the 2026-06-16 Red court refunded the money but left the Notion rows on `Confirmed` and the parent un-emailed. Two gaps: (1) `handleChargeRefunded` mapped the refund→row via `stripe.checkout.sessions.list({payment_intent})`, which can come back empty → silent `skipped:"no_checkout"`, no flip; (2) the coach "Refund + cancel" button's already-refunded detection (`/already.*refund/i`) didn't match Stripe's actual over-refund error ("…greater than unrefunded amount" / "…minus refunds already issued"), so it bailed "nothing changed" instead of flipping.
- **Decision:** Map the refund straight to its row by the Payment Intent we already persist on every row — fewer hops, no Stripe round-trip, fully offline-testable. New `findDropInPageByPaymentIntentId` (read-only) + `cancelDropInByPaymentIntent` (extracted shared `cancelDropInRow` core; existing `cancelDropIn(checkoutSessionId,…)` byte-unchanged). `handleChargeRefunded` now calls the PI entrypoint. For the coach button, replaced the regex with `isAlreadyRefundedError()` (code `charge_already_refunded` + message variants), so an already-refunded charge is treated as success and the row still flips + emails.
- **Risk:** Slop-free payments + minor-PII (`cancel-dropin.ts`, `notion-dropins.ts`, stripe webhook, `refund-amount.ts`, coach actions) — IPAV. No new child-PII egress (same row, same parent-only comms). `cancelDropIn` behavior preserved (parity spec still green). Webhook handler is now fetch-only on the refund path → testable; idempotent on redelivery via the existing status check. NOTE flagged to Sam: if the prod endpoint isn't subscribed to `charge.refunded` in Stripe, the code fix alone won't fire — that's a dashboard toggle to verify.
- **Change:** `src/lib/notion-dropins.ts` (+findByPI), `src/lib/cancel-dropin.ts` (refactor + cancelDropInByPaymentIntent), `src/app/api/stripe/webhook/route.ts` (handleChargeRefunded → PI lookup), `src/lib/refund-amount.ts` (+isAlreadyRefundedError), `src/app/coach/(authed)/[slug]/actions.ts` (use predicate); tests `e2e/webhook-charge-refunded.spec.ts` (4, signed events, mutation-verified — wrong-status flip fails 2) + `e2e/refund-amount.spec.ts` (+6 isAlreadyRefundedError, mutation-verified — naive regex fails 3). Full `npm run test:pure` (624) + lint + tsc green.
