// Locations rotate seasonally — see /schedule and lead form.
// This component is intentionally a no-op now; kept to avoid breaking imports
// during the wind-down. Delete once all consumers stop importing it.
export default function LocationMap() {
  return null;
}
